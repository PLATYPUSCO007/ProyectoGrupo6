import { CodigoActivacionDirective } from './codigo-activacion.directive';

describe('CodigoActivacionDirective', () => {
  it('should create an instance', () => {
    const directive = new CodigoActivacionDirective();
    expect(directive).toBeTruthy();
  });
});
